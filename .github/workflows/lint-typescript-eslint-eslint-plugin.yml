# This file is auto-generated. See ci/generate-workflows.js
name: typescript-eslint-eslint-plugin

on:
    workflow_dispatch: # Manual triggers
    workflow_run:
        workflows:
            - Run all plugin workflows
        types:
            - completed
    schedule:
        # Every thursday at 15:00
        - cron: '0 15 * * THU'

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v2
              with:
                  node-version: 14
            - run: |
                  yarn install
                  yarn build
                  rm -rf ./node_modules
            - run: |
                  yarn install
                  yarn build
                  rm -rf ./node_modules
              working-directory: ./repositories
            - run: |
                  yarn install
                  yarn list | grep eslint
                  yarn log --config ./plugin-configs/typescript-eslint-eslint-plugin.config.js
              working-directory: ./ci
            - uses: AriPerkkio/eslint-remote-tester-run-action@v1
              with:
                  working-directory: ./ci
                  issue-title: 'Weekly scheduled smoke test: typescript-eslint-eslint-plugin'
                  eslint-remote-tester-config: plugin-configs/typescript-eslint-eslint-plugin.config.js

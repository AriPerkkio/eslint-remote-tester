# This file is auto-generated. See ci/generate-workflows.js
name: eslint-plugin-jest

on:
  workflow_dispatch: # Manual triggers
  workflow_run:
    workflows:
      - Run all plugin workflows
    types:
      - completed
  schedule:
    # Every thursday at 02:00
    - cron: '0 02 * * THU'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12.11
    - run: yarn install
    - run: yarn build
    - run: yarn install
      working-directory: ./ci
    - run: "yarn list | grep eslint"
      working-directory: ./ci
    - run: "yarn log --config ./plugin-configs/eslint-plugin-jest.config.js"
      working-directory: ./ci
    - run: "yarn lint --config ./plugin-configs/eslint-plugin-jest.config.js"
      working-directory: ./ci
      env:
        CI: true

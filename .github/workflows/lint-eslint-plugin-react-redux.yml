# This file is auto-generated. See ci/generate-workflows.js
name: Lint eslint-plugin-react-redux

on:
  workflow_dispatch: # Manual triggers
  workflow_run:
    workflows:
      - Run all plugin workflows
    types:
      - completed
  schedule:
    # Every thursday at 09:00
    - cron: '0 09 * * THU'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12.11
    - run: yarn install
    - run: yarn build
    - run: yarn install --no-lockfile
      working-directory: ./ci
    - run: "yarn lint --config ./plugin-configs/eslint-plugin-react-redux.config.js"
      working-directory: ./ci
      env:
        CI: true

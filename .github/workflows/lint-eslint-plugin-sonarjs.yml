# This file is auto-generated. See ci/generate-workflows.js
name: Lint eslint-plugin-sonarjs

on:
  workflow_dispatch:
  schedule:
    # Every thursday at 10:00
    - cron: '0 10 * * THU'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12.11
    - run: yarn install
    - run: yarn build
    - run: yarn install --no-lockfile
      working-directory: ./ci
    - run: "yarn lint --config ./plugin-configs/eslint-plugin-sonarjs.config.js"
      working-directory: ./ci
      env:
        CI: true
